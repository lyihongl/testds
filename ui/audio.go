package ui

// Audio sits in the presentation layer (ADR 0003). The binding is ebiten's
// Oto, which the ui package already carries. No audio is implemented yet —
// it lands with the render layer work.
