// Command coredefense is the entry point of the Go build of the game.
//
// The playable loop currently lives in the Python prototype
// (prototype_game_loop.py) — that file is the behavior reference until the
// architecture tickets on "Chart the extensible game base" resolve and the
// real build is handed off. This stub exists to keep the toolchain honest.
package main

import "fmt"

func main() {
	fmt.Println("CORE DEFENSE — Go base ready")
}
